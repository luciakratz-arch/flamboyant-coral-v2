[build]
builder = "NIXPACKS"
buildCommand = "npm install -g pnpm@8 && /root/.npm-global/bin/pnpm install --shamefully-hoist && /root/.npm-global/bin/pnpm --filter api-server run build"

[deploy]
startCommand = "node artifacts/api-server/dist/index.mjs"
